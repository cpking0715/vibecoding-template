---
description: "严格 TDD 工作流（Red-Green-Refactor，小步快跑，禁止提前写业务代码）通用模板，首次使用时自动同步项目上下文"
globs: "*.ts, *.tsx, *.py, *.go"
---
<!-- 权威来源: VIBECODING-RULES.md，本文件为 Cursor 适配精简版 -->
# TDD 工作流（测试驱动开发）

严格 TDD 流程，绝对禁止一次性生成完整业务代码。

## 严格工作流程

1. **需求拆解** → Micro-steps（五原则：一个 step = 一个 Given-When-Then，≤3 个 test case，不可再分，≤2 个文件，先骨架后肉）
2. **验收标准** → Given-When-Then
3. **Red** → 仅写测试。生成后立即停止，并明确提示用户："测试代码已生成，请运行测试并告诉我结果，或授权我运行它。"
4. **等待确认** → 在用户反馈测试结果（或确认测试失败）之前，绝对禁止编写任何业务实现代码。
5. **Green** → 仅写能让刚才失败的测试通过的最少业务代码。
6. **运行验证** → 运行测试确保全部通过。如失败，分析输出并修复直到通过。
7. **Refactor** → 在测试通过的保护下，消除重复、提升可读性（只做 extract/rename/simplify），再次确认测试通过。
8. **更新文档** → git commit
9. **循环** → 完成当前单元后，询问用户是否进入下一个单元。

## 绝对禁令
- 🚫 禁止在测试验证通过前写业务代码
- 🚫 禁止跳过测试运行
- 🚫 禁止一次性处理过大需求

## 测试策略
- **优先**：纯函数（工具、校验、格式化、数据转换）
- **暂不优先**：UI 组件、页面样式、外部集成
- **框架**：由 TDD 上下文同步自动检测
- **隔离**：mock 外部依赖，禁依赖真实数据库
